package binarek.robio.common.persistence;

public interface EntityFetchProperties {

    final class NotSupported implements EntityFetchProperties {
        private NotSupported() {
        }
    }
}
