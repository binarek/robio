package binarek.robio.core.domain.team;

import binarek.robio.common.persistence.EntityFetchProperties;

public enum TeamFetchLevel implements EntityFetchProperties {
    TEAM_BASIC_INFO,
    TEAM,
}
